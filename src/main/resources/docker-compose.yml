services:
  postgres:
    image: postgres:latest
    container_name: aad_db_container
    restart: always
    environment:
      POSTGRES_DB: aad_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql
